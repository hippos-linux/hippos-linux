# Controller overlay images

Place PNG images here for bezel tattoo compositing.

Filename convention: `{system}.png` or `generic.png` (fallback).

Used when `global.bezel.tattoo = system` in hippos.conf.
Size: any — resized to ~225px wide relative to bezel canvas.
Format: RGBA PNG preferred.
