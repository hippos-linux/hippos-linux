# Emulator Packages

Future emulator build recipes, patches, and packaging scripts belong here.

Do not commit downloaded upstream source trees or compiled emulator outputs.
Those generated files should be staged under `artifacts/emulators/` by
`build/build-emulators.sh`.
